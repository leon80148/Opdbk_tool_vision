# 安家診所掛號助手 (Anchia Clinic Registration Assistant) v4.3.2

> 基於 Electron + React 的桌面應用程式，協助診所掛號櫃檯提供智慧化病患資訊整合與預防保健管理。

---

## 📁 專案結構

```
anchia-opd-toolkit/
├── config/                          # 設定檔目錄
│   ├── lab_codes.json              # 檢驗項目代碼對應表
│   └── rules.example.json          # 規則定義範例
├── config.ini.example              # 設定檔範例
├── data/                           # DBF 資料檔案
│   ├── CO01M.DBF                   # 病患主資料
│   ├── CO02M.DBF                   # 處方記錄
│   ├── CO03M.DBF                   # 帳務摘要
│   ├── CO18H.DBF                   # 檢驗歷史
│   └── VISHFAM.DBF                 # 家戶名單
├── logs/                           # 日誌目錄
├── src/
│   ├── main/                       # Electron 主程式
│   │   ├── main.js                 # 應用程式入口
│   │   ├── preload.js              # 預載腳本（IPC 橋接）
│   │   ├── logger.js               # 日誌模組
│   │   └── config-manager.js       # 設定管理器
│   ├── db/                         # 資料庫存取層
│   │   ├── database-manager.js     # SQLite 資料庫管理
│   │   ├── dbf-reader.js           # DBF 讀取器（ODBC）
│   │   └── sync-manager.js         # 資料同步管理
│   ├── shared/                     # 共用程式碼
│   │   └── utils.js                # 工具函式
│   └── renderer/                   # React 前端
│       ├── main.jsx                # React 入口
│       ├── App.jsx                 # 主應用程式元件
│       ├── App.css                 # 應用程式樣式
│       ├── index.css               # 全域樣式
│       └── components/             # UI 元件
│           ├── SearchBar.jsx       # 病歷號搜尋列
│           ├── PatientHeader.jsx   # 病患基本資料
│           ├── SmartActionList.jsx # 待辦事項清單
│           ├── LabMatrix.jsx       # 檢驗矩陣
│           ├── ClinicalJourney.jsx # 診療歷程
│           └── SyncStatus.jsx      # 同步狀態
├── index.html                      # HTML 入口
├── vite.config.js                  # Vite 建構配置
├── package.json                    # 專案相依設定
├── database_schema.MD              # 資料庫架構文件
├── OPD_TOOLIT_PRD.MD              # 產品需求文件
└── README.md                       # 本檔案

```

---

## 🚀 快速開始

### 前置需求

1. **Node.js 18+ 與 npm**
   - 下載：https://nodejs.org/

2. **Visual FoxPro ODBC Driver**（用於讀取 DBF 檔案）
   - Windows 10/11 內建，或下載 Microsoft Visual FoxPro OLE DB Provider

3. **DBF 測試資料**
   - 確認 `data/` 目錄下有 5 個 DBF 檔案

---

### 安裝步驟

#### 步驟 1：安裝相依套件

```bash
cd D:\programing\github\anchia_opd_toolit
npm install
```

#### 步驟 2：建立設定檔

```bash
# 複製 config.ini 範例
copy config.ini.example config.ini

# 複製規則定義範例
copy config\rules.example.json config\rules.json
```

#### 步驟 3：編輯 config.ini

開啟 `config.ini`，確認以下設定：

```ini
[database]
dbf_root = D:\\programing\\github\\anchia_opd_toolit\\data\\
sqlite_path = D:\\programing\\github\\anchia_opd_toolit\\data\\lab_cache.db

[labs]
lab_code_map = D:\\programing\\github\\anchia_opd_toolit\\config\\lab_codes.json

[rules]
rules_definition = D:\\programing\\github\\anchia_opd_toolit\\config\\rules.json
```

> **注意**：Windows 路徑需使用雙反斜線 `\\` 或單斜線 `/`。

---

### 執行應用程式

#### 開發模式

```bash
npm run dev
```

這會同時啟動：
- **Vite 開發伺服器**（React 前端）於 `http://localhost:5173`
- **Electron 應用程式**（載入開發伺服器）

#### 建置生產版本

```bash
npm run build          # 建置 React 前端
npm run build:win      # 建置 Windows 安裝程式
```

建置完成後，安裝程式位於 `dist/` 目錄。

---

## 🔑 關鍵功能

### 1. 病患資料查詢
- 輸入病歷號（自動補零至 7 碼）
- 查詢基本資料（姓名、性別、年齡、聯絡方式）
- 顯示 VIP 標籤與特殊註記

### 2. Smart Action List（待辦事項清單）
- 自動評估規則，產生待辦事項
- 包含：
  - **衛教提醒**（HBA1C > 7.0 等）
  - **檢查追蹤**（超音波、內視鏡逾期）
  - **資料補填**（聯絡資料缺漏）
- 依優先級排序（1 = 最高）

### 3. 檢驗矩陣
- **DM 區**：HBA1C、UACR、eGFR
- **HTN/LIP 區**：CHOL、LDL-C、TG、BMI
- **Virus 區**：Anti-HCV、HBsAg
- 顯示最新數值與檢驗日期
- 顏色標示（紅/橘/綠）

### 4. 診療歷程
- 上次就診記錄
- 上次領藥記錄

### 5. 資料同步
- 初始匯入：從 CO18H 匯入 2-3 年檢驗資料
- 增量同步：每 10 分鐘自動同步新資料
- 手動同步：可點擊「手動同步」按鈕

### 6. 全域熱鍵
- 預設：`Ctrl + Alt + C`
- 從任何畫面喚出助手
- 可在 config.ini 自訂熱鍵

---

## ⚙️ 設定檔說明

### config.ini

完整的設定項目請參考 `config.ini.example`。主要設定包括：

| 區塊 | 設定項目 | 說明 | 預設值 |
|------|---------|------|--------|
| `[database]` | `dbf_root` | DBF 資料庫根目錄 | `data/` |
| `[database]` | `sqlite_path` | SQLite 快取資料庫路徑 | `data/lab_cache.db` |
| `[hotkey]` | `global` | 全域熱鍵組合 | `Ctrl+Alt+C` |
| `[sync]` | `interval_minutes` | 同步間隔（分鐘） | `10` |
| `[labs]` | `lab_code_map` | LabCodeMap 檔案路徑 | `config/lab_codes.json` |
| `[rules]` | `rules_definition` | 規則定義檔路徑 | `config/rules.json` |

### lab_codes.json

定義檢驗項目代碼與顯示名稱、正常值範圍、警示閾值。

**格式範例：**

```json
{
  "DM": {
    "items": {
      "HBA1C": {
        "hitem_code": "HBA1C",
        "display_name": "糖化血色素",
        "unit": "%",
        "normal_range": { "min": 4.0, "max": 6.0 },
        "thresholds": {
          "normal": 6.0,
          "warning": 7.0,
          "critical": 9.0
        }
      }
    }
  }
}
```

> **重要**：`hitem_code` 必須與 DBF 檔案中的 `CO18H.hitem` 欄位值一致。

### rules.json

定義 Smart Action List 的規則邏輯。

**格式範例：**

```json
{
  "rules": [
    {
      "id": "dm_education_critical",
      "type": "education",
      "priority": 1,
      "condition": {
        "field": "HBA1C",
        "operator": ">=",
        "value": 9.0
      },
      "action": {
        "icon": "🔴",
        "color": "red",
        "title": "需安排糖尿病衛教（高風險）"
      }
    }
  ]
}
```

---

## 🗄️ 資料庫架構

### DBF 資料表（來源：HIS 系統）

| 資料表 | 說明 | 關鍵欄位 |
|--------|------|----------|
| `CO01M` | 病患主資料 | `kcstmr`, `mname`, `mtelh`, `maddr` |
| `CO02M` | 處方/醫令記錄 | `kcstmr`, `idate`, `dno`, `ptday` |
| `CO03M` | 帳務/診斷摘要 | `kcstmr`, `idate`, `labno` |
| `CO18H` | 檢驗歷史（垂直表） | `kcstmr`, `hitem`, `hdate`, `hval` |
| `VISHFAM` | 家戶名單 | `pat_pid` |

### SQLite 資料表（本機快取）

| 資料表 | 說明 | 用途 |
|--------|------|------|
| `lab_results_raw` | 原始檢驗快取 | 儲存從 CO18H 抓下來的檢驗紀錄 |
| `lab_results_wide` | 寬表（供前端查詢） | 提供前端一次取得各類檢驗的最新值 |
| `sync_meta` | 同步狀態紀錄 | 記錄同步進度與狀態 |

---

## 🔧 開發指南

### 專案技術棧

- **Desktop**：Electron 28+
- **Frontend**：React 18.2 + Ant Design 5.x
- **Backend (Main Process)**：Node.js
- **資料存取**：ODBC + `better-sqlite3`
- **編碼轉換**：`iconv-lite`（Big5 ↔ UTF-8）
- **定時任務**：`node-cron`
- **日誌**：`winston`
- **建構工具**：Vite

### 主要流程

#### 1. 應用程式啟動流程

```
main.js (Electron Main Process)
  ↓
載入 config.ini (ConfigManager)
  ↓
建立主視窗 (BrowserWindow)
  ↓
註冊全域熱鍵 (globalShortcut)
  ↓
初始化資料庫 (DatabaseManager)
  ↓
初始化同步管理器 (SyncManager)
  ↓
啟動時同步（可選）
  ↓
啟動定期同步（可選）
```

#### 2. 病患查詢流程

```
SearchBar (React Component)
  ↓
window.electronAPI.queryPatient(patientId)
  ↓
IPC Main: 'query-patient'
  ↓
DatabaseManager.queryPatient()
  ├── DBFReader.queryPatientBasicInfo() → CO01M
  ├── DatabaseManager.queryLabMatrix() → SQLite
  ├── DBFReader.queryClinicalHistory() → CO02M/CO03M
  └── DatabaseManager.evaluateRules() → Smart Action List
  ↓
返回 React 前端顯示
```

#### 3. 資料同步流程

```
SyncManager.runSync()
  ↓
檢查 sync_meta.last_hdate_synced
  ├── 空值 → initialImport()（初始匯入）
  └── 有值 → incrementalSync()（增量同步）
  ↓
查詢 CO18H（批次查詢）
  ↓
寫入 lab_results_raw
  ↓
更新 lab_results_wide（轉置為寬表）
  ↓
更新 sync_meta
```

---

## 🐛 常見問題

### Q1: npm install 失敗，提示 better-sqlite3 編譯錯誤

**解決方案：**
- 安裝 Visual Studio Build Tools
- 或使用預編譯版本：
  ```bash
  npm install better-sqlite3 --build-from-source
  ```

### Q2: 無法連線到 DBF 檔案

**檢查項目：**
1. 確認 Visual FoxPro ODBC Driver 已安裝
2. 確認 `config.ini` 中的 `dbf_root` 路徑正確
3. 確認 DBF 檔案沒有被其他程式鎖定

### Q3: 中文顯示亂碼

**解決方案：**
- 確認 `config.ini` 中的 `encoding` 設定為 `big5`
- 檢查 `dbf-reader.js` 的編碼轉換邏輯

### Q4: 全域熱鍵無法註冊

**解決方案：**
- 檢查熱鍵是否與其他程式衝突
- 修改 `config.ini` 中的 `hotkey.global` 設定
- 確認 `hotkey.conflict_handling` 設定（預設為 `warn`）

### Q5: 初始匯入花費時間過長

**優化建議：**
- 減少 `data_retention_years`（預設 3 年）
- 增加 `batch_size`（預設 5000）
- 關閉啟動時同步：設定 `sync_on_startup = false`

---

## 📝 開發待辦事項

以下是尚未完成的功能（可視需求實作）：

### Phase 1：核心功能補強
- [ ] **完整規則引擎**
  - 目前 `database-manager.js` 中的 `evaluateRules()` 僅為示範
  - 需實作完整的規則載入、評估、快取機制
  - 支援複雜條件（AND/OR/NOT）

- [ ] **檢查追蹤功能**
  - 查詢 CO02M 取得檢查記錄（超音波、內視鏡）
  - 計算是否逾期
  - 產生追蹤提醒

- [ ] **疫苗推廣功能**
  - 查詢 CO02M 取得疫苗記錄
  - 依年齡與施打歷史推薦疫苗

### Phase 2：UI/UX 改善
- [ ] **檢驗矩陣顏色標示**
  - 依據 LabCodeMap 的 thresholds 判斷顏色
  - 實作 Tag color（綠/橘/紅）

- [ ] **Smart Action List 展開詳細資訊**
  - 點擊後顯示建議動作（suggested_actions）
  - 提供「完成」或「忽略」按鈕

- [ ] **未來預約顯示**
  - 查詢 HIS 系統的預約資料（需確認資料表）

- [ ] **錯誤訊息提示**
  - 使用 Ant Design 的 `message` 或 `notification`
  - 顯示查詢失敗、同步失敗等錯誤

### Phase 3：效能優化
- [ ] **查詢快取機制**
  - 快取最近查詢的病患資料
  - 設定 TTL（預設 5 分鐘）

- [ ] **SQLite 索引優化**
  - 分析查詢效能
  - 新增必要的索引

- [ ] **進度回饋**
  - 初始匯入時顯示進度條
  - 同步時顯示當前進度

### Phase 4：進階功能
- [ ] **使用者帳號與權限**
  - 實作登入機制
  - 區分櫃檯、醫師、管理員權限

- [ ] **多診所支援**
  - 支援多組 DBF 路徑
  - 診所切換功能

- [ ] **LabCodeMap 管理介面**
  - 可視化編輯檢驗項目對應
  - 匯入/匯出功能

- [ ] **衛教單列印**
  - 整合衛教單樣板
  - 自動填入病患資料

---

## 📄 相關文件

- **產品需求文件**：[OPD_TOOLIT_PRD.MD](./OPD_TOOLIT_PRD.MD)
- **資料庫架構文件**：[database_schema.MD](./database_schema.MD)
- **LabCodeMap 範例**：[config/lab_codes.json](./config/lab_codes.json)
- **規則定義範例**：[config/rules.example.json](./config/rules.example.json)
- **設定檔範例**：[config.ini.example](./config.ini.example)

---

## 📞 支援

如有問題或建議，請聯絡開發團隊。

---

## 📜 授權

MIT License

---

**安家診所掛號助手 v4.3.2** - 讓診所照護更智慧、更有溫度 ❤️
